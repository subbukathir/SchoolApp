<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Background color(white) -->
    <color name="color_Primary">#F1F2F2</color>

    <!-- Notification bar color -->
    <color name="color_Primary_Dark">#921B19</color>

    <!-- Text color(App theme Red) -->
    <color name="color_Accent">#BE1E2D</color>

    <!-- color_WindowBackground -->
    <color name="color_WindowBackground">#F6F7F7</color>

    <!-- Favorites ex_rate_background -->
    <color name="color_WindowBackground_Dark">#EBECEC</color>

    <!-- Reprocess Country Color and In transaction list currency and amount color -->
    <color name="color_grey_dark">#6F7179</color>

    <!-- Button color(Red) -->
    <color name="color_button_Red">#9A1414</color>

    <!-- color red -->
    <color name="color_red">#FF0000</color>

    <!-- Background color(White) -->
    <color name="color_bg_white">#FFFFFF</color>

    <!-- Text color(White) -->
    <color name="color_text_white">#FFFFFF</color>

    <!-- Text color(Yellow) -->
    <color name="color_text_yellow">#FCCD11</color>

    <!-- Text color(Black) -->
    <color name="color_text_black">#0F0C0C</color>

    <!-- Text color(Black) -->
    <color name="color_black">#111111</color>

    <!-- Text color Exchange rates list-All Rates -->
    <color name="color_ex_rates_list_view_text_color">#747879</color>

    <!-- Tabhost Background color(light_grey) -->
    <color name="color_tabhost_background">#dbd9da</color>

    <!-- Tabhost Background color(light_grey) -->
    <color name="color_background_grey">#dbd9da</color>

    <!-- Tabhost Text color(black) -->
    <color name="color_tabhost_text_black">#0F0C0C</color>

    <!-- Tabhost Text color(Red) -->
    <color name="color_tabhost_text_red">#9A1414</color>

    <!-- Update button color(Green) -->
    <color name="color_updatebutton_green">#3F783F</color>

    <!-- change account button color(Blue) -->
    <color name="color_changeAccountbutton_blue">#3A67AB</color>
    <color name="color_light_blue">#f0f7f6</color>

    <!-- login edit text background color -->
    <color name="color_login_edittext">#e8e5e5</color>

    <!-- login edit recipient background color -->
    <color name="color_recipient_background">#f5f5f5</color>

    <!-- transaction list progress color -->
    <color name="color_transaction_yellow">#FF9900</color>

    <!-- transaction list progress color -->
    <color name="color_bg_green">#ACE1AF</color>

    <color name="color_button_grey">#939393</color>

    <color name="color_header_bg_grey">#939597</color>
</resources>